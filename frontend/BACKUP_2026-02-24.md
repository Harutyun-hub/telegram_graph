# Armenian Telegram Intelligence Platform - Backup
**Date:** February 24, 2026
**Version:** Multi-page Dashboard v1.0

## System Architecture

### New File Structure
```
/src/app/
  ├── App.tsx (RouterProvider entry point)
  ├── routes.tsx (React Router configuration)
  ├── layouts/
  │   └── AdminLayout.tsx (Main dashboard layout with sidebar)
  ├── pages/
  │   ├── DashboardPage.tsx (AI Intelligence Briefing - Main page)
  │   ├── PostsPage.tsx (Telegram posts monitoring)
  │   ├── GraphPage.tsx (Graph visualization - moved from App.tsx)
  │   ├── AchievementsPage.tsx (User achievements)
  │   ├── KeywordsPage.tsx (Trending keywords & topics)
  │   └── SettingsPage.tsx (Account & database settings)
  └── components/
      ├── Logo.tsx (NEW - Cyber teal/coral logo)
      ├── GlobalFiltersLight.tsx (NEW - Light theme filters for graph)
      ├── GraphVisualization.tsx (UPDATED - Light background)
      ├── GlobalFilters.tsx (Original dark theme - still used)
      └── [other existing components]
```

## Key Changes Made

### 1. Routing System (NEW)
- **File:** `/src/app/routes.tsx`
- **Description:** React Router configuration with 6 main routes
- **Routes:**
  - `/` - DashboardPage (Intelligence Briefing)
  - `/posts` - PostsPage
  - `/graph` - GraphPage
  - `/achievements` - AchievementsPage
  - `/keywords` - KeywordsPage
  - `/settings` - SettingsPage

### 2. App.tsx (COMPLETELY REWRITTEN)
- **Before:** Full graph visualization dashboard
- **After:** Simple RouterProvider wrapper
- **Old Logic:** Moved to `/src/app/pages/GraphPage.tsx`

### 3. AdminLayout (NEW)
- **File:** `/src/app/layouts/AdminLayout.tsx`
- **Features:**
  - Slate-800 dark sidebar
  - Logo at top
  - Navigation menu with icons
  - User profile at bottom
  - White top header with search bar
  - Notification bell
  - Outlet for page content

### 4. Logo Component (NEW)
- **File:** `/src/app/components/Logo.tsx`
- **Features:**
  - Cyber teal to coral gradient
  - SVG-based with glow effects
  - Perspective grid design
  - Animated data nodes
  - Two variants: Logo (horizontal) and LogoIcon (square)
  - Matches glassmorphism aesthetic

### 5. DashboardPage (NEW)
- **File:** `/src/app/pages/DashboardPage.tsx`
- **Features:**
  - AI Intelligence Briefing section
  - 4 stat cards (Total Comments, Active Channels, Trust in Gov, Migration Intent)
  - Sentiment trends chart (Area chart)
  - Sources distribution (Pie chart)
  - Topic trends (Horizontal bar chart)
  - Geopolitical stances (Progress bars)
  - Achievement banner
  - Time range selector (Day/Week/Month)

### 6. PostsPage (NEW)
- **File:** `/src/app/pages/PostsPage.tsx`
- **Features:**
  - Filter categories (#US Election, #Global Warming, #AI future, #Technology)
  - Active filters display
  - Sort by dropdown
  - Post cards with:
    - Channel info & icon
    - Post text
    - Tags
    - Topics & Sentiment
    - Geopolitical stance
    - Engagement stats (likes, comments, shares)
    - Reply button

### 7. GraphPage (NEW - MOVED LOGIC)
- **File:** `/src/app/pages/GraphPage.tsx`
- **Description:** Original graph visualization moved here
- **Features:**
  - All original graph functionality
  - GlobalFiltersLight sidebar
  - NodeInspector
  - FloatingControls
  - GraphLegend
  - ExportButton

### 8. GlobalFiltersLight (NEW)
- **File:** `/src/app/components/GlobalFiltersLight.tsx`
- **Description:** Light-themed version of GlobalFilters
- **Colors:**
  - White background
  - Gray borders
  - Blue accents (instead of cyan)
  - Gray text (instead of white)
- **Features:** Same functionality as dark version

### 9. GraphVisualization (UPDATED)
- **File:** `/src/app/components/GraphVisualization.tsx`
- **Changes:**
  - backgroundColor: `#f9fafb` (light gray instead of dark)
  - Link colors updated:
    - Selected: `rgba(59, 130, 246, 0.6)` (blue)
    - Hover: `rgba(99, 102, 241, 0.5)` (indigo)
    - Default: `rgba(156, 163, 175, 0.3)` (light gray)

### 10. KeywordsPage (NEW)
- **File:** `/src/app/pages/KeywordsPage.tsx`
- **Features:**
  - 3 summary cards (Total Keywords, Trending Topics, Period)
  - Keywords table with columns:
    - Keyword (with # icon)
    - Category tag
    - Mentions count
    - Trend indicator (Rising/Falling/Stable)
    - Change percentage

### 11. AchievementsPage (NEW)
- **File:** `/src/app/pages/AchievementsPage.tsx`
- **Features:**
  - Achievement cards grid
  - Progress bars
  - Unlocked/Locked states
  - Icons: Trophy, Star, Award, Target
  - Color coding (blue, yellow, green, purple)

### 12. SettingsPage (NEW)
- **File:** `/src/app/pages/SettingsPage.tsx`
- **Sections:**
  - Profile Settings (name, email, role)
  - Notification Settings (toggles)
  - Database Connections (Neo4j & Supabase status)
  - Save/Cancel buttons

## Color Palette Changes

### Old (Dark Theme - Graph Only)
- Background: `#0B0E14` (dark navy)
- Accent: Cyan/Teal (`#06b6d4`)
- Secondary: Coral/Orange (`#f97316`)

### New (Light Theme - Admin Dashboard)
- Background: `#f9fafb` (gray-50)
- Sidebar: `#1e293b` (slate-800)
- Accents: Blue (`#3b82f6`)
- Borders: `#e5e7eb` (gray-200)
- Text: `#111827` (gray-900)

### Graph Page (Light Theme)
- Background: `#f9fafb`
- Links: Gray (`rgba(156, 163, 175, 0.3)`)
- Selected Links: Blue (`rgba(59, 130, 246, 0.6)`)
- Nodes: Keep original vibrant colors (they work on light backgrounds)

## Package Dependencies Added
- `react-router@^7.13.1`

## Original Graph Components (PRESERVED)
All original graph functionality preserved:
- GraphVisualization.tsx
- GlobalFilters.tsx (dark theme)
- NodeInspector.tsx
- FloatingControls.tsx
- GraphLegend.tsx
- ExportButton.tsx
- EmptyGraphState.tsx
- DotMatrixBackground.tsx
- ConnectionTest.tsx
- AIQueryBar.tsx
- DateRangePicker.tsx

## API Services (UNCHANGED)
- `/src/app/services/api.ts` - All Neo4j & Supabase calls preserved

## Mock Data Used
- DashboardPage: Mock sentiment/topic/geopolitical data
- PostsPage: Mock Telegram posts (3 examples)
- KeywordsPage: Mock keyword trends (8 examples)
- AchievementsPage: Mock achievements (4 examples)

## Navigation Menu Items
1. Dashboard - `LayoutDashboard` icon - Count: 125
2. Posts - `FileText` icon - Count: 126
3. Graph - `Share2` icon - No count
4. Achievements - `Trophy` icon - No count
5. Keywords - `Tag` icon - No count
6. Settings - `Settings` icon - No count

## User Profile
- Name: "Admin User"
- Email: "admin@armentel.io"
- Avatar: User icon with gradient background
- Status: "You Replied to 0 posts!"

## Restore Instructions
If you need to restore the original single-page graph dashboard:

1. Replace `/src/app/App.tsx` with this:
```tsx
import { useState, useRef } from 'react';
import { GraphVisualization } from '@/app/components/GraphVisualization';
import { AIQueryBar } from '@/app/components/AIQueryBar';
import { GlobalFilters } from '@/app/components/GlobalFilters';
import { NodeInspector } from '@/app/components/NodeInspector';
import { FloatingControls } from '@/app/components/FloatingControls';
import { GraphLegend } from '@/app/components/GraphLegend';
import { DotMatrixBackground } from '@/app/components/DotMatrixBackground';
import { ExportButton } from '@/app/components/ExportButton';

function App() {
  const [selectedNode, setSelectedNode] = useState<any>(null);
  const [filters, setFilters] = useState<{
    brands?: string[];
    sentiments?: string[];
    timeframe?: string;
  }>({});
  const [graphData, setGraphData] = useState<any>(null);
  const [allNodes, setAllNodes] = useState<any[]>([]);
  const graphRef = useRef<any>(null);

  // ... (copy all handler functions from GraphPage.tsx)

  return (
    <div className="relative w-screen h-screen overflow-hidden bg-[#0B0E14]">
      <DotMatrixBackground />
      <AIQueryBar />
      <ExportButton onExport={handleExportImage} />
      <GlobalFilters
        onFiltersChange={handleFiltersChange}
        onQuickSelectBrand={handleQuickSelectBrand}
        onSearchSelect={handleSearchSelect}
        allNodes={allNodes}
      />
      <GraphVisualization
        ref={graphRef}
        onNodeClick={handleNodeClick}
        filters={filters}
        onDataUpdate={handleGraphDataUpdate}
      />
      <FloatingControls graphRef={graphRef} />
      <GraphLegend />
      {selectedNode && (
        <NodeInspector
          node={selectedNode}
          onClose={handleCloseInspector}
          onQuickSelectBrand={handleQuickSelectBrand}
        />
      )}
    </div>
  );
}

export default App;
```

2. Remove or comment out the router import
3. Revert GraphVisualization backgroundColor to `"transparent"`

## Testing Checklist
- [ ] Navigation between all pages works
- [ ] Graph page filters work with light theme
- [ ] Dashboard charts render correctly
- [ ] Posts page filters toggle properly
- [ ] Settings page form inputs work
- [ ] Logo displays correctly in sidebar and query bar
- [ ] Responsive layout on mobile
- [ ] Neo4j data loads in graph page
- [ ] Export functionality works

## Notes
- All original graph functionality preserved and moved to `/graph` route
- Dark theme graph is still available via `GlobalFilters.tsx`
- Light theme optimized for executive dashboard
- Mock data can be replaced with real API calls later
- Logo uses SVG for crisp scaling
- Recharts library used for all charts
